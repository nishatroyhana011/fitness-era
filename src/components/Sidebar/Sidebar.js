import React from 'react';

const Sidebar = (props) => {
    console.log(props)
   // const duration = props.duration;
    return (
        <div>
            <p>I am sidebar</p>
           
        </div>
    );
};

export default Sidebar;